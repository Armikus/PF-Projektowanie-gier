using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class drawCard : MonoBehaviour
{
 /*   public GameObject Card;
    public GameObject PlayerHand;

    private playerDeckControler cardController;

    void Start() {
        GameObject player = GameObject.Find("Player");
        cardController = player.GetComponent<playerDeckControler>();
    }

    public void addNewCardToHand() {
        GameObject playerCard;
        Card temp;
        for (int i = 0; i < cardController.handSize; i++) {
            playerCard = Instantiate(Card, new Vector3(0, 0, 0), Quaternion.identity);
            temp = cardController.drawCard();
            temp.fillTemplate(playerCard);

            playerCard.transform.localScale += new Vector3(0.5F, 0.5F, 0.5F);
            playerCard.transform.SetParent(PlayerHand.transform, false);
        }

    }*/
}
